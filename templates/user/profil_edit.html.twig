{% extends 'base.html.twig' %}

{% block add1 %}
    {# app.user.picture#}

{% endblock %}

{% block add2 %}
    {{ form_start(editForm) }}
    {{ form_widget(editForm) }}
    {{ form_end(editForm) }}
{% endblock %}

